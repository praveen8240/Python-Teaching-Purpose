import Link from "next/link";import {sessions} from "@/content/sessions";import {ArrowRight,Clock3,CalendarDays} from "lucide-react";
export default function Sessions(){const days=[...new Set(sessions.map(s=>s.day))];return <main className="mx-auto max-w-[1100px] px-5 py-12"><header className="mb-10"><p className="text-xs font-black uppercase text-[#196b4b]">Curriculum map</p><h1 className="mt-2 text-4xl font-black sm:text-5xl">Available sessions</h1><p className="mt-3 max-w-2xl text-[#607067]">Three complete classroom sessions. Progress is saved in this browser.</p></header>{days.map(day=><section key={day} className="mb-10"><div className="mb-4 flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-md bg-[#17211b] text-white"><CalendarDays size={18}/></span><div><h2 className="text-xl font-black">Day {day}</h2><p className="text-sm text-[#607067]">{sessions.filter(s=>s.day===day).length} session{sessions.filter(s=>s.day===day).length>1?"s":""}</p></div></div><div className="space-y-3">{sessions.filter(s=>s.day===day).map(s=><Link href={`/session/${s.id}`} key={s.id} className="card group grid gap-5 p-5 transition hover:border-[#7fad8e] md:grid-cols-[70px_1fr_auto] md:items-center"><span className="grid h-14 w-14 place-items-center rounded-md bg-[#edf3ee] text-xl font-black">{String(s.id).padStart(2,"0")}</span><span><strong className="block text-xl">{s.title}</strong><span className="mt-1 block text-sm text-[#607067]">{s.focus.join(" · ")}</span></span><span className="flex items-center gap-4"><span className="flex items-center gap-1 text-sm text-[#607067]"><Clock3 size={15}/>{s.duration}</span><ArrowRight className="transition group-hover:translate-x-1"/></span></Link>)}</div></section>)}</main>}
